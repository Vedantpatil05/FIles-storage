<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Upload - DriveCloud</title>

  <!-- Bootstrap 5 CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />

  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #f8f9fa;
    }
    .logo {
      font-weight: 700;
      font-size: 24px;
      color: #0d6efd;
    }
    .upload-card {
      max-width: 420px;
      margin: 0 auto;
      padding: 2rem;
      border-radius: 10px;
      background-color: #ffffff;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
    }
  </style>
</head>
<body>

  <div class="container py-5">

    <!-- Logo/Header -->
    <div class="text-center mb-4">
      <div class="logo">📤 FilesStorage</div>
      <p class="text-muted small">Upload your personal files securely</p>
    </div>

    <!-- Upload Box -->
    <div class="upload-card">
      <h4 class="mb-3 text-center text-dark">Upload a File</h4>

      <% if (success) { %>
        <div class="alert alert-success text-center py-2 mb-3">
          ✅ Upload successful!
        </div>
      <% } %>

      <form action="/upload" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <input class="form-control" type="file" name="file" required />
        </div>
        <button type="submit" class="btn btn-primary w-100">Upload</button>
      </form>

      <div class="text-center mt-4">
        <a href="/home" class="text-decoration-none text-primary">⬅️ Go to My Files</a>
      </div>
    </div>

  </div>

  <!-- Bootstrap JS (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
